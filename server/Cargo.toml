[package]
name = "webserver"
version = "0.1.0"
edition = "2021"
# Liten och snabb HTTP-server utan onödigt lull-lull

[dependencies]

rouille = "3.6.2"

[profile.release]
opt-level = "z"     
lto = true          
codegen-units = 1   
panic = "abort"     
strip = true        